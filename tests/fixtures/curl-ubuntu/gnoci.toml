cmd = ["curl", "--head", "https://google.com"]

[[entries]]
source = "/usr/bin/curl"
target = "/usr/bin/curl"

# An absolute directory entry containing symlinks and files owned by OS packages
[[entries]]
source = "/etc/ssl/certs/"
target = "/etc/ssl/certs/"

# A relative directory entry
[[entries]]
source = "./conf"
target = "/etc/foo"
